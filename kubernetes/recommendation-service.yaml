apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: coursematch

spec:
  selector:
    app: recommendation
  ports:
    - name: http
      protocol: TCP
      port: 8004
      targetPort: 8004
  type: ClusterIP
